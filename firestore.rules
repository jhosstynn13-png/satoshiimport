rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For now, allow read/write to authenticated users for simplicity
      // and read access to all for the catalog items.
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
